# Strapi CMS
STRAPI_URL=http://localhost:1337
STRAPI_API_TOKEN=

# On-demand ISR revalidation (called by a Strapi webhook on publish/update)
REVALIDATE_SECRET=

# Public site URL (used for metadata, sitemap, JSON-LD)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Newsletter API route
# (writes to Strapi's newsletter-signup collection — no separate key needed,
# authenticated via STRAPI_API_TOKEN above)

# External weather API (Open-Meteo — no API key required)
